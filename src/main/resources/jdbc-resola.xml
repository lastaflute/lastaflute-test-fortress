<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE components PUBLIC "-//DBFLUTE//DTD LastaDi 1.0//EN"
	"http://dbflute.org/meta/lastadi10.dtd">
<components namespace="jdbc_resola">
    <!-- not use embedded holder instance as sub schema
     <include path="plugin/selectable_datasource.xml"/>
    -->
    <include path="jdbc-resola-master.xml"/>
    <include path="jdbc-resola-slave.xml"/>

    <!-- these component names are basically unused so non-unique names are allowed
     attention: but if you use by-name injection, set unique name here
     e.g. resolaDataSource, resolaSlaveDBAccessor (if schema nickname is "resola")
     -->
    <component name="dataSource" class="org.lastaflute.db.replication.slavedb.MasterBasisSelectableDataSource"/>
    <component name="slaveDBAccessor" class="org.docksidestage.bizfw.masterslave.resortlinedb.ResortlineSlaveDBAccessor"/>
    <component name="selectableDataSourceHolder" class="org.docksidestage.bizfw.masterslave.resortlinedb.ResortlineDBSelectableDataSourceHolder"/>
</components>
